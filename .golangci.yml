version: "2"
run:
  timeout: 5m
linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - revive
    - ineffassign
    - unused
  exclusions:
    rules:
      - path: '_test\.go'
        linters:
          - errcheck
formatters:
  enable:
    - goimports
